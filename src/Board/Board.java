package Board;

import java.awt.*;

public class Board {

    private int x, y, tileWidth, tileHeight, numTileWidth, numTileHeight;

    public Board(int x, int y, int tileWidth, int tileHeight, int numTileWidth, int numTileHeight) {
        this.x = x;
        this.y = y;
        this.tileWidth = tileWidth;
        this.tileHeight = tileHeight;
        this.numTileWidth = numTileWidth;
        this.numTileHeight = numTileHeight;

    }

    public void tick() {

    }

    public void render(Graphics g) {

    }
}
